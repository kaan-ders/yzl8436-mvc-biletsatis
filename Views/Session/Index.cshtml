@{
}

@model List<Session>

<h3>Seanslar</h3>

<div class="text-end">
    <a href="/Session/Add" class="btn btn-primary">Ekle</a>
</div>

<table class="table mt-3">
    <tr>
        <th>
            Tarih
        </th>
        <th>
            Film
        </th>
        <th>
            Salon
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.DateTime
            </td>
            <td>
                @item.Movie.Title
            </td>
            <td>
                @item.Saloon.No
            </td>
            <td><a href="#">düzenle</a> | <a href="#">sil</a></td>
        </tr>
    }
</table>